// Εμβαδόν ενός κύκλου με PI * p^2
const radius = 5;

const area = Math.PI * radius ** 2;

console.log(`Circle with radius ${radius}cm has area ${area.toFixed(2)}cm\u00B2`);  //u00 κάνει εκθέτη κάτι και Β το συμβολο ή τον αριθμό που θα βάλω! useful.
